# Implementation Plan: パッケージマネージャーをpnpmに移行

**Branch**: `003-switch-to-pnpm` | **Date**: 2026-04-23 | **Spec**: [spec.md](./spec.md)
**Input**: Feature specification from `specs/003-switch-to-pnpm/spec.md`

## Summary

Migrate the project's package manager from npm to pnpm. Remove
package-lock.json, generate pnpm-lock.yaml, verify all existing
functionality works, and add a guard to prevent accidental npm usage.

## Technical Context

**Language/Version**: TypeScript 6+ (strict mode)
**Primary Dependencies**: React 19, Vite 8, TailwindCSS v4, anime.js 4
**Storage**: N/A
**Testing**: Manual browser testing + build verification
**Target Platform**: Local development environment
**Project Type**: Single-page web application (frontend only)
**Performance Goals**: pnpm install < 30s, pnpm build error-free
**Constraints**: All existing dependencies must remain at same versions
**Scale/Scope**: 1 lockfile swap, 1 config file, build verification

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1.*

| Principle | Status | Notes |
|-----------|--------|-------|
| I. Component-First Architecture | ✅ PASS | No component changes |
| II. Type Safety | ✅ PASS | No type changes |
| III. Performance & User Experience | ✅ PASS | No runtime changes |
| IV. Responsive & Accessible Design | ✅ PASS | No UI changes |
| V. Simplicity & Maintainability | ✅ PASS | pnpm is simpler (strict deps, faster installs). Constitution says "npm or pnpm (choose one, do not mix)" — switching to pnpm is compliant |

All gates pass.

## Project Structure

### Documentation (this feature)

```text
specs/003-switch-to-pnpm/
├── plan.md
├── research.md
├── quickstart.md
└── checklists/
    └── requirements.md
```

### Files Changed (repository root)

```text
(root)
├── package.json           # Add "packageManager" field + preinstall guard
├── pnpm-lock.yaml         # NEW — generated by pnpm install
├── package-lock.json      # DELETED
├── node_modules/          # Recreated by pnpm
└── .npmrc                 # OPTIONAL — pnpm config if needed
```

**Structure Decision**: No source code changes. Only package manager
configuration files at the repository root.

## Complexity Tracking

No violations — table not needed.
